#!/usr/bin/with-contenv bashio

echo "Starting Bun TypeScript Application..."

# Navigate to the app directory
cd /app

# Run your production script (make sure this script is defined in your package.json)
# E.g., "start": "bun run src/index.ts"
bun run prod